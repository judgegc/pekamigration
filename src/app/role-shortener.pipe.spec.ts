import { RoleShortenerPipe } from './role-shortener.pipe';

describe('RoleShortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleShortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
